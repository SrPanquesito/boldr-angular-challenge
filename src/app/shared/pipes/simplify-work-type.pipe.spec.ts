import { SimplifyWorkTypePipe } from './simplify-work-type.pipe';

describe('SimplifyWorkTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SimplifyWorkTypePipe();
    expect(pipe).toBeTruthy();
  });
});
